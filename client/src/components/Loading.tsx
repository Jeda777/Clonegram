import { Center, Spinner } from '@chakra-ui/react'

const Loading = () => {
  return (
    <Center width='100%' height='100vh'>
      <Spinner size='xl' />
    </Center>
  )
}

export default Loading
